# Fable-GPT Skill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tạo skill `ops/fable-gpt` — orchestrator "Claude điều khiển, Codex thực hiện" với adaptive routing + eval static, theo spec đã duyệt `docs/superpowers/specs/2026-07-17-fable-gpt-design.md`.

**Architecture:** Phương án A — single-file thin orchestrator. Một `SKILL.md` duy nhất chứa Security Gate → Classifier → Fast Path / Full Cycle (Plan → Review → CEO Gate → Execute → Verify), dispatch Codex qua Agent tool `subagent_type: "codex:codex-rescue"`. Eval `mode: "static"` (regex audit SKILL.md). Không sub-block, không vendor script.

**Tech Stack:** Markdown SKILL.md (KN-Stack convention) · plugin `openai-codex` 1.0.6 (Agent tool) · `evals/run-eval.sh` static mode · `setup.sh` junction deploy.

## Global Constraints

- KHÔNG BAO GIỜ commit vào main. Toàn bộ việc trên branch mới `feature/ops-fable-gpt` (tách từ HEAD hiện tại của `feature/design-wx-krpano`).
- Repo đang có thay đổi chưa commit của việc KHÁC (`skills/book/book-ceo-insight/SKILL.md`, `skills/galaxy/research/references/exa-discovery.md`, `skills/ops/nlm/SKILL.md`, `evals/fixtures/`, `skills/design/wx-krpano/references/PROJECT_PLAYBOOK.md`, `temp/`). TUYỆT ĐỐI không `git add` các file đó — chỉ add đích danh file của task này.
- Commit format: `[OPS] <mô tả>` + trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- KHÔNG bump `VERSION` / `CHANGELOG.md` — CEO chốt thời điểm (đang có bump khác pending). Ghi chú lại ở báo cáo cuối.
- Frontmatter SKILL.md: `name:` = tên thư mục (`fable-gpt`), `description:` có triggers song ngữ (convention CLAUDE.md repo).
- Model IDs chính xác (đã smoke-test 2026-07-17): `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`; effort hợp lệ của codex:rescue: `none|minimal|low|medium|high|xhigh`.
- Tiếng Việt cho nội dung nội bộ, thuật ngữ kỹ thuật giữ tiếng Anh (đúng giọng các SKILL.md hiện có).

---

### Task 1: Branch mới + commit design spec

**Files:**
- Commit (đã tồn tại): `docs/superpowers/specs/2026-07-17-fable-gpt-design.md`

**Interfaces:**
- Consumes: HEAD hiện tại của `feature/design-wx-krpano`.
- Produces: branch `feature/ops-fable-gpt` — mọi task sau chạy trên branch này.

- [ ] **Step 1: Tạo branch**

```bash
cd /d/KN-Stack && git checkout -b feature/ops-fable-gpt
```

Expected: `Switched to a new branch 'feature/ops-fable-gpt'`

- [ ] **Step 2: Commit spec (chỉ add đích danh file spec)**

```bash
git add docs/superpowers/specs/2026-07-17-fable-gpt-design.md
git commit -m "[OPS] Add fable-gpt design spec (Claude-orchestrator x Codex-executor)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

Expected: 1 file changed. Chạy `git status` xác nhận các file dở dang của việc khác VẪN ở trạng thái modified/untracked (không bị cuốn vào commit).

---

### Task 2: Eval spec trước (failing test)

**Files:**
- Create: `evals/fable-gpt.json`

**Interfaces:**
- Consumes: format static-mode của `evals/run-eval.sh` (regex case-insensitive trên SKILL.md, resolve qua `~/.claude/commands/fable-gpt/SKILL.md`).
- Produces: 12 assertions mà Task 3 phải làm SKILL.md thỏa. ID prefix `FG-`.

- [ ] **Step 1: Viết eval spec**

Ghi `evals/fable-gpt.json` với nội dung chính xác:

```json
{
  "skill": "fable-gpt",
  "version": "1.0",
  "description": "Binary assertions for fable-gpt — Claude-orchestrator × Codex-executor adaptive pipeline (Plan/Review/CEO-Gate/Execute/Verify)",
  "mode": "static",
  "test_input": "refactor module X chạm 6 file, đổi interface — giao codex theo chu trình fable-gpt",
  "total_required": 12,
  "passing_score": 11,
  "assertions": [
    {
      "id": "FG-SECURITY-GATE",
      "name": "security_gate_before_any_dispatch",
      "check": "Security Gate MẬT/HẠN-CHẾ chạy TRƯỚC mọi dispatch Codex (cloud OpenAI); bị chặn → 2 lối ra abstract/local",
      "regex": "Security Gate.*(TRƯỚC|trước|first)|MẬT.*(DỪNG|dừng|không dispatch)",
      "required": true
    },
    {
      "id": "FG-CLASSIFIER",
      "name": "classifier_three_criteria_with_reason",
      "check": "classifier 3 tiêu chí (phạm vi rõ / số file / rủi ro kiến trúc) + nêu 1 câu lý do phân loại",
      "regex": "3 tiêu chí|phạm vi.*(file|kiến trúc)|rủi ro kiến trúc",
      "required": true
    },
    {
      "id": "FG-FAST-PATH",
      "name": "fast_path_sol_medium_default",
      "check": "FAST PATH giao thẳng gpt-5.6-sol effort mặc định (không set --effort), nhảy đến Verify",
      "regex": "FAST PATH.*gpt-5\\.6-sol|gpt-5\\.6-sol.*(medium|mặc định)",
      "required": true
    },
    {
      "id": "FG-PLAN-XHIGH",
      "name": "plan_step_sol_xhigh_readonly",
      "check": "bước Plan dùng gpt-5.6-sol --effort xhigh và read-only (planning only, không sửa file)",
      "regex": "xhigh.*(read-only|planning|không.*sửa)|--effort xhigh",
      "required": true
    },
    {
      "id": "FG-REVIEW-FABLE",
      "name": "review_by_claude_reads_real_repo",
      "check": "bước Review do Claude/Fable làm inline, PHẢI đọc file thật mà plan nhắc đến (không tin plan mù)",
      "regex": "Review.*(Claude|Fable).*(đọc|inline)|đọc.*file.*plan",
      "required": true
    },
    {
      "id": "FG-CEO-GATE",
      "name": "ceo_gate_default_auto_flag",
      "check": "CEO Gate mặc định DỪNG trình plan; flag --auto bỏ gate",
      "regex": "CEO Gate.*(mặc định|DỪNG|dừng).*|--auto.*(bỏ|skip)",
      "required": true
    },
    {
      "id": "FG-EXECUTOR-TERRA",
      "name": "executor_terra_default_flag_switch",
      "check": "Execute mặc định gpt-5.6-terra, flag --executor <terra|luna|sol> để đổi",
      "regex": "gpt-5\\.6-terra.*(mặc định|default)|--executor",
      "required": true
    },
    {
      "id": "FG-VERIFY-LOOP",
      "name": "verify_max_two_fix_rounds_resume",
      "check": "Verify: FAIL → fix-hints qua --resume giữ thread, TỐI ĐA 2 vòng, sau đó dừng báo CEO với root-cause",
      "regex": "(tối đa|TỐI ĐA) 2 vòng|fix.?hints.*--resume",
      "required": true
    },
    {
      "id": "FG-DIFF-SCOPE",
      "name": "out_of_scope_diff_red_flag",
      "check": "diff chạm file ngoài phạm vi plan đã duyệt → cờ đỏ, không chấp nhận im lặng",
      "regex": "ngoài phạm vi.*(cờ đỏ|red flag)|cờ đỏ.*(diff|file)",
      "required": true
    },
    {
      "id": "FG-STATE-FILE",
      "name": "state_file_resume_never_commit",
      "check": "state file _fablegpt_state.md tại repo đích, --resume đọc lại, xóa khi xong, KHÔNG BAO GIỜ commit",
      "regex": "_fablegpt_state",
      "required": true
    },
    {
      "id": "FG-DISPATCH-AGENT",
      "name": "dispatch_via_codex_rescue_subagent",
      "check": "mọi dispatch qua Agent tool subagent_type codex:codex-rescue — KHÔNG gọi codex-companion trực tiếp, KHÔNG vendor path plugin",
      "regex": "codex:codex-rescue",
      "required": true
    },
    {
      "id": "FG-ERROR-NO-BLIND-RETRY",
      "name": "codex_dead_no_blind_retry",
      "check": "Codex CLI chết/timeout → báo ngay + /codex:status, KHÔNG retry mù",
      "regex": "(KHÔNG|không).*retry mù|codex:status",
      "required": true
    }
  ]
}
```

- [ ] **Step 2: Chạy eval để xác nhận FAIL (skill chưa tồn tại)**

```bash
cd /d/KN-Stack && bash evals/run-eval.sh fable-gpt
```

Expected: `ERROR: Skill file not found for: fable-gpt` (exit ≠ 0) — đây là trạng thái "failing test" đúng nghĩa.

- [ ] **Step 3: Commit eval spec**

```bash
git add evals/fable-gpt.json
git commit -m "[OPS] Add fable-gpt static eval spec (12 assertions)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

Expected: 1 file changed.

---

### Task 3: SKILL.md + deploy junction + eval xanh

**Files:**
- Create: `skills/ops/fable-gpt/SKILL.md`

**Interfaces:**
- Consumes: 12 assertion FG-* của Task 2; plugin contract `codex:codex-rescue` (flags `--model`, `--effort`, `--resume`, `--fresh`, `--background`, `--wait`).
- Produces: skill `/fable-gpt` deployed qua junction, eval 12/12.

- [ ] **Step 1: Viết SKILL.md**

Ghi `skills/ops/fable-gpt/SKILL.md` với nội dung chính xác:

````markdown
---
name: fable-gpt
description: "Fable-GPT orchestrator — Claude (Fable 5) điều khiển, Codex (GPT-5.6) thực hiện. Adaptive routing: task nhỏ/rõ → FAST PATH gpt-5.6-sol medium; task lớn/mơ hồ/kiến trúc → FULL CYCLE Plan(sol xhigh, read-only) → Review(Fable đọc repo thật) → CEO Gate → Execute(gpt-5.6-terra) → Verify(Fable, tối đa 2 vòng fix qua --resume). Security Gate MẬT chạy trước mọi dispatch. Flags: --fast, --full, --auto, --executor <terra|luna|sol>, --resume. Triggers on: 'fable-gpt', 'fable gpt', 'giao codex', 'delegate to codex', 'codex thực hiện', 'chu trình plan review execute', 'giao việc cho gpt', 'codex execute this', 'điều phối codex'."
---

# fable-gpt — Claude điều khiển · Codex thực hiện

> **Role:** thin commander. Claude (Fable 5) giữ phán đoán — phân loại, review plan,
> verify kết quả. Codex (GPT-5.6) giữ thực thi. Mọi dispatch đi qua Agent tool
> `subagent_type: "codex:codex-rescue"` (plugin OpenAI chính thức) — KHÔNG gọi
> codex-companion trực tiếp, KHÔNG vendor đường dẫn plugin (gãy khi plugin update).
> **Nguyên tắc bất di bất dịch:** không bao giờ chấp nhận kết quả Codex mà chưa tự verify.

## Model tiers (đã verify khả dụng 2026-07-17)

| Tầng | Model + effort | Vai trò |
|---|---|---|
| Xe hàng ngày | `gpt-5.6-sol` (effort mặc định medium — KHÔNG set `--effort`) | FAST PATH, ~80% việc |
| Lý luận nặng | `gpt-5.6-sol` + `--effort xhigh` | bước Plan của FULL CYCLE |
| Thực thi thuần | `gpt-5.6-terra` (mặc định) / `gpt-5.6-luna` | bước Execute |
| Phán đoán | Fable 5 (Claude, inline — không dispatch) | Classify, Review, Verify |

## Usage

```
/fable-gpt <nhiệm vụ>                       # adaptive routing
/fable-gpt <nhiệm vụ> --fast                # ép FAST PATH
/fable-gpt <nhiệm vụ> --full                # ép FULL CYCLE
/fable-gpt <nhiệm vụ> --full --auto         # full cycle, bỏ CEO Gate
/fable-gpt <nhiệm vụ> --executor luna       # đổi executor (terra|luna|sol)
/fable-gpt --resume                         # nối lại từ _fablegpt_state.md
```

## Step 0 — Security Gate (chạy TRƯỚC mọi dispatch, không ngoại lệ)

Codex = cloud OpenAI. Theo rule "No classified data in prompts" + pattern gate MẬT
của leo-assist/wx-krpano:

- Nhiệm vụ chạm nội dung MẬT/HẠN-CHẾ (geometry sản phẩm quốc phòng, thông số vũ khí,
  dữ liệu khách BQP, supplier pricing) → **DỪNG, không dispatch Codex.** Trình CEO
  2 lối ra: (a) abstract hóa — đổi tên/số liệu thành generic rồi chạy tiếp;
  (b) CEO tự làm local, không qua Codex.
- Task code thuần KN-Stack / generic / open-source → PASS.

## Step 0.5 — Classify (Claude inline, không tốn Codex)

Đánh giá theo **3 tiêu chí**: (1) phạm vi rõ chưa — file/hàm cụ thể vs mô tả mơ hồ;
(2) số file dự kiến chạm — ≤2 vs nhiều; (3) rủi ro kiến trúc — đổi interface/schema/
cross-domain. PHẢI nêu 1 câu lý do phân loại trong output (traceability).

| Kết quả | Đường đi |
|---|---|
| Nhỏ + rõ + ≤2 file + không kiến trúc | **FAST PATH** |
| Lớn / mơ hồ / đa file / kiến trúc | **FULL CYCLE** |

Override: `--fast` / `--full` thắng classifier.

## FAST PATH

1. Claude viết lại nhiệm vụ thành prompt cụ thể: phạm vi, file, tiêu chí done.
2. Dispatch Agent tool `subagent_type: "codex:codex-rescue"`, prompt bắt đầu bằng
   `--model gpt-5.6-sol` (KHÔNG set `--effort` — giữ medium mặc định) + task text.
3. Nhảy thẳng đến **Verify** (bước 5 của FULL CYCLE, cùng quy tắc 2 vòng).

## FULL CYCLE — 5 bước

| # | Bước | Ai | Model/effort |
|---|---|---|---|
| 1 | Plan | Codex read-only | `gpt-5.6-sol` + `--effort xhigh` |
| 2 | Review | Claude inline (Fable 5) | — |
| 3 | CEO Gate | CEO | — |
| 4 | Execute | Codex write | `gpt-5.6-terra` mặc định |
| 5 | Verify | Claude inline (Fable 5) | — |

### 1. Plan — sol xhigh, read-only
Dispatch `codex:codex-rescue` với prompt: `--model gpt-5.6-sol --effort xhigh` +
"PLANNING ONLY — do not edit any files, read-only analysis" + nhiệm vụ + ngữ cảnh
repo Claude đã nắm + yêu cầu output cấu trúc: steps / files chạm / edge cases /
done criteria.

### 2. Review — Fable đọc repo thật
Claude PHẢI đọc các file mà plan nhắc đến trước khi duyệt — không tin plan mù.
Soi: lỗ hổng, giả định sai, edge case thiếu, phạm vi phình. Sửa/bổ sung plan.
Plan sai hướng NẶNG → trả Codex đúng 1 lần kèm phản biện (dispatch `--resume` giữ
thread); vẫn lệch → Claude tự viết plan (Fable), ghi rõ nguồn plan trong báo cáo.

### 3. CEO Gate — mặc định DỪNG
Trình plan đã-review + các điểm Claude đã sửa. Chờ CEO duyệt rồi mới Execute.
`--auto` bỏ gate — chạy thẳng (CEO chấp nhận rủi ro cho task đó).

### 4. Execute — terra mặc định
Dispatch `codex:codex-rescue` với prompt: `--model gpt-5.6-terra` (hoặc model theo
`--executor luna|sol`) + plan đã khóa NGUYÊN VĂN + ranh giới cứng: "only touch
files listed in the plan". Task giao từng phần cụ thể, không giao cả cụm mơ hồ.

### 5. Verify — Fable, tối đa 2 vòng
- Đọc TOÀN BỘ diff (`git diff` / `git status`).
- Diff chạm file **ngoài phạm vi** plan đã duyệt → **cờ đỏ**: liệt kê file lạ,
  không chấp nhận im lặng, chờ CEO.
- Chạy test/eval liên quan — repo có gì chạy nấy (run-eval.sh, pytest, smoke).
- FAIL → viết fix-hints cụ thể → dispatch lại qua `--resume` (giữ thread Codex).
  **TỐI ĐA 2 vòng fix.** Sau 2 vòng vẫn FAIL → DỪNG, báo CEO: root-cause, diff
  hiện trạng, đề xuất (tự sửa tay / đổi hướng).
- PASS → báo cáo: tóm tắt diff, kết quả test, số vòng đã dùng, đường đã đi
  (fast/full + lý do phân loại).

## State & Resume

File `_fablegpt_state.md` tại thư mục gốc repo đích (KHÔNG phải scratchpad):

```markdown
task: <nhiệm vụ gốc>
route: fast | full
plan: <plan đã duyệt hoặc "n/a">
step: classify | plan | review | ceo-gate | execute | verify | done
fix_rounds: 0-2
codex_thread: open | closed
flags: <flags gốc>
```

- `/fable-gpt --resume` → đọc state, nối đúng bước; dispatch tiếp dùng `--resume`
  của codex:rescue để giữ thread.
- Xong việc (báo cáo hoàn thành) → XÓA state file.
- State file là artifact tạm — skill **KHÔNG BAO GIỜ commit** nó. Repo đích có
  `.gitignore` → kiểm tra `_fablegpt_state.md` đã ignore chưa, chưa có thì nhắc
  CEO (không tự sửa .gitignore của repo đích).

## Error handling

| Tình huống | Hành xử |
|---|---|
| Codex CLI chết / timeout | Báo ngay + gợi ý `/codex:status`, `/codex:setup`; **KHÔNG retry mù** |
| Verify FAIL 2 vòng | Dừng + báo cáo root-cause, diff, đề xuất |
| Plan sai hướng nặng | Trả Codex 1 lần → vẫn lệch → Claude tự viết plan |
| Diff ngoài phạm vi | Cờ đỏ trong Verify, liệt kê file lạ, chờ CEO |
| Security Gate chặn | Dừng + 2 lối ra (abstract / local) |

## Ngoài phạm vi (YAGNI)

Không parallel subagents · không tự động /checkpoint theo chu kỳ · không cost
tracking · không sub-block skills · không wrapper script.
````

- [ ] **Step 2: Deploy junction (idempotent — chỉ tạo junction mới, skip cái đã có)**

```bash
cd /d/KN-Stack && bash setup.sh --install
```

Expected: dòng `Skills: 1 linked, <N> skipped, 0 errors`.

- [ ] **Step 3: Verify junction resolve**

```bash
bash setup.sh --verify && ls ~/.claude/commands/fable-gpt/SKILL.md
```

Expected: verify OK, file resolve được qua junction.

- [ ] **Step 4: Chạy eval — phải xanh**

```bash
bash evals/run-eval.sh fable-gpt
```

Expected: `12/12` (hoặc ≥11/12 = passing_score, nhưng mục tiêu 12/12). Nếu assertion nào FAIL: sửa SKILL.md cho khớp `check` (KHÔNG nới regex để lách — regex chỉ được sửa khi chính nó sai chính tả/escape), chạy lại đến xanh.

- [ ] **Step 5: Commit SKILL.md**

```bash
git add skills/ops/fable-gpt/SKILL.md
git commit -m "[OPS] Add fable-gpt — Claude-orchestrator x Codex-executor skill (eval 12/12)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

Expected: 1 file changed.

---

### Task 4: Nghiệm thu cuối

**Files:** không tạo file mới — chỉ kiểm tra + báo cáo.

**Interfaces:**
- Consumes: kết quả Task 1–3.
- Produces: báo cáo nghiệm thu cho CEO; các mục deferred ghi rõ.

- [ ] **Step 1: Đếm skill + trạng thái deploy**

```bash
bash setup.sh --status | tail -20
```

Expected: tổng skill = 258 (257 + fable-gpt). Nếu số khác → đối chiếu `find skills -name SKILL.md | wc -l`, báo đúng số thực tế, không tự bịa.

- [ ] **Step 2: Xác nhận cây làm việc sạch (phần của mình)**

```bash
git status --short
```

Expected: CHỈ còn các file dở dang của việc khác (book-ceo-insight, exa-discovery, nlm, wx-krpano PROJECT_PLAYBOOK, evals/fixtures/, temp/) — không có file mới nào của fable-gpt còn untracked.

- [ ] **Step 3: Smoke thử skill mức nhẹ (không tốn Codex thật)**

Đọc lại `~/.claude/commands/fable-gpt/SKILL.md` qua junction và xác nhận frontmatter `name: fable-gpt` khớp tên thư mục; description có triggers. (Live-run full cycle với Codex thật để CEO tự quyết — tốn phí ChatGPT.)

- [ ] **Step 4: Báo cáo CEO**

Nội dung bắt buộc: eval score, branch + 3 commit, deferred items (VERSION/CHANGELOG bump chờ CEO; live-run full cycle chưa chạy; PR tạo khi CEO duyệt — lưu ý branch này stack lên `feature/design-wx-krpano`).

---

## Self-Review (đã chạy)

1. **Spec coverage:** Security Gate → Task 3 SKILL.md Step 0 ✓ · Classifier 3 tiêu chí + lý do ✓ · Fast path sol medium ✓ · Full cycle 5 bước đúng model map ✓ · CEO Gate + --auto ✓ · Verify 2 vòng + diff-scope ✓ · State/resume + không-commit ✓ · Error handling 5 tình huống ✓ · Frontmatter + triggers ✓ · Eval static 12 assertions ✓ · Deploy junction ✓ · Đếm 258 ✓ · YAGNI exclusions ✓. VERSION/CHANGELOG: cố ý deferred (Global Constraints).
2. **Placeholder scan:** không còn TBD/TODO; mọi bước có lệnh + expected output; SKILL.md và JSON đầy đủ nguyên văn.
3. **Type consistency:** tên `fable-gpt` / `_fablegpt_state.md` / ID `FG-*` / flags `--fast --full --auto --executor --resume` thống nhất giữa spec, eval JSON, SKILL.md.
