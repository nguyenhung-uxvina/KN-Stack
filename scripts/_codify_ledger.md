# Codify Ledger — append-only registry of markdown→Python codifications

| Date | Target Skill | Script | LOC | Tests | Annual LLM saving (est.) | CEO sign-off |
|------|-------------|--------|-----|-------|--------------------------|--------------|
| 2026-06-22 | mech-drawing-extract | scripts/extract/parse_mech_drawing.py | ~520 | manual (real DXF + regex unit checks) | DXF/PDF parse done in code, not re-derived per run; field-hardened on VTI GT.00.01.01 | pending |
| 2026-06-30 | doc-to-json | scripts/extract/parse_document.py | ~650 | manual (live fixtures + real BM-01 QTCN docx: headings, nested lists, tables, runs, xlsx formulas, xlsx+docx merged cells via known-answer fixture, pages all verified) | PDF/MD/DOCX/XLSX → unified JSON parsed in code, not re-transcribed per run; format auto-detect + fidelity flags + DOCX table merge-span dedup (gridSpan/vMerge) | pending |
| 2026-07-01 | qtcn (--json / --cad) | scripts/helix/qtcn_to_json.py | ~380 | manual (real BM-01 tong-the.md: 28 NC A/B/C parsed; labor/materials/equipment/BOM/torque; auto-mined 23 acceptance checkpoints; scope=B → 17 NC; --cad parses 14 IV blocks → cad_illustration, flag off ⇒ required:false; known-answer on B11 torque+thẳng đứng+callouts) | QTCN MD → verification JSON parsed in code, not hand-transcribed per run; auto-extracts quantitative acceptance checkpoints (mô-men/dung sai/Δ/khoảng cách/điện áp/IP) from YCKT incl. sub-bullets; reads mục IV → cad_illustration spec | pending |
| 2026-07-01 | qtcn / product-dossier (seed) | scripts/extract/extract_to_qtcn_seed.py | ~180 | manual (real BM-01 extracted/: BOM.json + 23 trang-*.json → 17 VT parts, assembly excluded, 12 consumables; sections L40×40×4/L30×30×3/hộp30×60×1,8/tấm15→plate15 + tolerances 3060±20/850±10 mined; flags tcvn3+pdf_scanned propagated) | extract-cad JSON → qtcn-seed.json (nền tảng QTCN) biến đổi tất định trong code: gán VT tự động, gộp vật liệu/SL, rút section/plate/tolerances/key-dims per part, không nhập tay BOM/VT mỗi lần | pending |
| 2026-07-01 | qtcn (figure register) | scripts/helix/qtcn_figure_register.py | ~150 | manual (real BM-01 JSON: 28 hình A/B/C → bảng danh mục + bảng tổng hợp bản vẽ nguồn; expand dải 02.00–08.00 → mọi bản vẽ trung gian; append idempotent qua marker; 15 bản vẽ nguồn + 5 hình dựng mới) | QTCN JSON (--cad) → PHỤ LỤC sổ đăng ký hình sinh tự động, khớp tuyệt đối thân QTCN; gom bản vẽ nguồn để họa viên trích 1 lượt; không gõ tay 28 dòng | pending |
| 2026-07-01 | mech-drawing-extract (3D engine) | scripts/extract/freecad_extract.py | ~215 | **verified trên FreeCAD 1.1.1 (freecadcmd) + STEP fixture** (tấm 1000×500×20 + 2 trụ r50h300): total_mass 115,492 kg khớp tuyệt đối (78,5 + 2×18,496); gộp instance qty=2; **dedup container** (bỏ compound assembly, hết đếm trùng 2×); nhận diện vật liệu từ nhãn (assumed=False). Env-var invocation (FC_FILE/FC_OUT/FC_NAME — tránh freecadcmd nuốt args); entry-guard chạy dù __name__≠__main__; cp1252-safe. Known-answer PASS. **Field-hardened trên assembly THẬT Tong lap.stp (BM-01, 279 solid)**: sửa 2 bug lộ ra — (1) base_label bỏ sót hậu tố STEP zero-padded dính liền (…duoi)001 → 236 nhóm ảo, nay gộp đúng **21 nhóm** (Than phao×2, L40×40×4×38, Nẹp ngang×44…); (2) total_mass dùng unit×qty SAI cho thanh Frame Generator cùng profile khác chiều dài → nay **tổng THẬT = Σ khối lượng instance** (715,865 kg khớp tuyệt đối tổng 279 solid), thêm mass_varies + unit_mass_range_kg. (3) STEP gán nhãn vật liệu placeholder 'Default' → mọi part thành thép, phao composite nống ~4,6×; thêm **suy vật liệu theo TÊN chi tiết** (NAME_MATERIAL: than phao→composite 1,70; hõm gỗ→gỗ 0,55; còn lại→Thép EH32 7,85) + bỏ qua nhãn placeholder Default/Generic + `material_source` (label/name_rule/default) + flag material_sources. Kết quả BM-01: **408,065 kg** (Than phao×2 composite 85 kg thay vì 393 kg thép). Ghi nhận: .ipt/.iam Inventor FreeCAD KHÔNG mở được (cần STEP export) | FreeCAD headless (freecadcmd) đọc solid 3D → qtcn-seed.json cùng schema: khối lượng THẬT (V×ρ, density map theo vật liệu), bbox, area, gộp instance→BOM qty (Σ thật khi khác khối lượng), dedup container; vá điểm yếu est_mass=[CẦN BÓC TÁCH] khi có mô hình 3D; engine split 2D→ezdxf / 3D→FreeCAD | pending |
| 2026-07-01 | qtcn / product-dossier (merge) | scripts/extract/merge_qtcn_seeds.py | ~210 | manual (BM-01: seed2D 17 chi tiết + seed3D 21 solid → khớp mờ bỏ dấu + BẮT BUỘC cùng danh từ đầu + Jaccard, ngưỡng 0,72: **6 both** (Thân phao, Cột, Nẹp dọc/ngang/dọc-ngắn, Mã cạnh phao↔Ma tai phao 0,78 cần rà), 11 only_2d (dầm/cánh là cụm hàn), 15 only_3d (bích/mã/tai/thanh profile); 1 qty_mismatch Cột 4vs1; false match dầm↔bích/cánh↔mã bị chặn nhờ head-token) | gộp seed 2D(drawing_no+dung sai+section)+3D(khối lượng thật+SL) → 1 seed đầy đủ; khớp mờ có phân biệt lớp chi tiết, giữ + gắn cờ provenance phần chỉ-một-nguồn thay vì ép khớp sai | pending |
| 2026-07-02 | WX-QT-EXTRACT-SENSOR-01 (harness trích xuất) | scripts/extract/validate_qtcn_seed.py | ~430 | verified trên fixture cài lỗi chủ đích: seed tốt PASS exit 0; seed xấu bắt đúng **7/7 FAIL đúng mã rule** (S1-08 nhầm đơn vị ×1000, S1-02 vật liệu ngoài danh mục, S1-05 đẳng chu, S1-06 V>bbox, S1-03 bbox>envelope, S1-07 qty null, S1-04 Σmass≠total) + S4-03 thiếu sót; S2 cross Inventor↔FreeCAD (12,8% FAIL, unmatched WARNING, QTY WARNING); S4-01 determinism PASS/FAIL; S4-02 diff revision; **G3 golden set** (case trong dung sai PASS, thiếu actual FAIL); utf-8-sig chịu BOM; đ→d normalize | Sensor 4 lớp + Gate G1/G3 của đặc tả chạy bằng code trên qtcn-seed/v1, không phụ thuộc CAD nguồn; seed FAIL không validated → 5 đầu ra từ chối; xuất validation_report (= biểu mẫu F01) | pending |
| 2026-07-02 | Inventor headless (Apprentice adapter) | scripts/extract/inventor_apprentice_extract.py | ~230 | **CHƯA chạy thật** (môi trường không có Inventor/Inventor View) — py_compile OK + đường lỗi exit 3 OK; theo G3: cấm dùng sản xuất tới khi qua golden set nhánh inventor/ | Batch trích .ipt/.iam không mở Inventor UI, không tốn seat license (chỉ cần Inventor View); đọc iProperties cache (mass/volume/material/PN/rev) + SHA-256 truy vết + sensor S1 nhúng + G1 exit code; QTY=1 (Apprentice không có occurrence count) — QTY thật vẫn từ bom_xlsx_to_seed.py qua merge | pending |
| 2026-07-02 | P0 hội đồng CAD-AI: schema + release-gate + golden v0 | schemas/qtcn-seed.schema.json + validate_qtcn_seed.py (nâng cấp) + golden/make_golden_v0.py + self_check.bat | ~200 mới | **G3 chạy THẬT end-to-end**: 3 case giải tích (plate-4holes/box-tube/cylinder) dựng bằng FreeCAD 1.1.1 → STEP → freecad_extract → so đáp án tính tay: PASS 3/3 (mass 38,633/4,427/12,331 kg khớp tuyệt đối); golden run đầu bắt lỗi thật Part.export mất nhãn → chuyển Import.export; structural check (jsonschema 2020-12) bắt trường lạ cấp gốc + thiếu vt/qty + volume âm; --release chặn khi thiếu --cross; fields() hỗ trợ seed merged (qty_3d/material_3d fallback); self_check.bat kéo-thả PASS | Thi hành mục 2.1 đặc tả bằng máy (schema khóa cứng); đóng lỗ "bất biến tự chấm mình" (S2 bắt buộc khi phát hành); golden set từ khuyến nghị thành hạ tầng đang chạy | pending |
| 2026-07-02 | freecad_extract bug #3 (bắt bởi SENSOR trên field data) + golden case 4 | scripts/extract/freecad_extract.py (vá) + golden/step/frame-varies/ | ~15 sửa | **S1-01 tự bắt trên Tong lap.iam THẬT** (STEP từ rule iLogic mới): 3 nhóm mass_varies FAIL tới 794% — est_mass_kg lấy instance NHẸ NHẤT nhưng volume/bbox lấy instance ĐẦU TIÊN (ISO L40x40x4 est 0,133 kg vs vol thanh dài 151,52 cm³). Vá: instance đại diện nhóm = nhẹ nhất (est/vol/bbox CÙNG 1 thanh). Sau vá: Tong lap 279 solid/21 nhóm/408,065 kg giữ nguyên, VERDICT WARNING-only (21 vật liệu suy đoán — chờ BOM.csv); golden case frame-varies (2 thanh giải tích cùng tên khác dài) vào bộ vĩnh viễn, G3 PASS 4/4 | Bug 2 lần field-hardening thủ công trước không thấy — sensor tự động thấy ngay lần chạy đầu; đúng luận điểm "chứng minh script đúng" thay vì "tin script" | pending |
| 2026-07-02 | Apprentice adapter — chạy SỐNG lần đầu (Tong lap.iam, Inventor 2018) | scripts/extract/inventor_apprentice_extract.py 0.1.0 | ~10 sửa | ProgID thật = "Inventor.ApprenticeServer" (tài liệu API ghi "...ServerComponent" — sai trên bản cài 2018; adapter thử cả 2). Live: 49 part duy nhất, 49/49 có Part Number, SHA-256 OK. **Chẩn đoán đắt nhất: 33/49 Material='Generic' (16 thanh thép hình Frame Generator có 'Steel, Mild' thật — đính chính số 49/49 báo lần đầu do output bị cắt), 0/49 mass cache, 0/49 revision** — vật liệu chưa từng gán trong CAD ⇒ mass Inventor (kể cả BOM view: Than phao 25,02 kg = V×ρ Generic 1,0) KHÔNG dùng được cho file legacy này; số khối lượng đúng duy nhất = V(STEP) × ρ(vật liệu từ bản vẽ 2D) qua merge_qtcn_seeds. iLogic BOM: view Parts Only/Structured bị Disabled trong tài liệu → fallback Model Data (level-1) hoạt động | Sensor S1-02 nhúng trong adapter chặn đúng (exit≠0, validated=false); xác nhận luận điểm Guides tầng bản vẽ: thư viện vật liệu khóa cứng phải có TRƯỚC khi BOM export thành nguồn vàng | pending |
| 2026-07-09 | WX-QT-DRAWING-SENSOR-01 lớp D1+SEC (harness tầng bản vẽ) | scripts/extract/drawing_check.py | ~230 | **chạy sống ngay trên Tong lap.iam** (Apprentice, 49 part): VERDICT FAIL 240/WARNING 49 đúng baseline B.4 — D1-02×33 (Generic; 16 thép hình Frame Generator có 'Steel, Mild' thật), D1-03×49 (không revision), D1-06×49 (không mass cache), D1-09×11 (skeleton/KT lọt BOM), D1-10×49 (chưa có WX_PartType), SEC-01×49; xuất drawing_report.json (=F01) + **giao-viec.csv** (phiếu giao việc mở Excel, cột "cần bổ sung" per part) | Gate 1 tầng bản vẽ chạy bằng máy: bản vẽ FAIL không được Released → không vào pipeline trích xuất; self-check rẻ (không cần mở Inventor); tái dùng get_prop/walk_assembly của adapter + danh mục vật liệu của validator — 1 bộ hạ tầng, 2 tầng harness | pending |
| 2026-07-13 | U0 độ bền vận hành: battery + hook + 1 nguồn vật liệu + gate cưỡng chế | scripts/extract/run_battery.py + scripts/hooks/pre-commit + schemas/materials.json + vá validate/freecad_extract/merge | ~180 mới | battery 6/6 ĐẠT (fixture cài lỗi kỳ vọng exit-code + đếm FAIL=7 + golden regen qua freecadcmd); **hook chạy thật trong commit 0860f34** (chặn được commit hỏng harness, --no-verify để override có chủ đích); gate merge test 2 chiều: từ chối exit 4 khi seed chưa validate → PASS sau validate; refactor materials.json không lệch kết quả nào (battery xanh trước/sau) | Đóng 3 rủi ro độ bền: (1) công trình đã COMMIT + tag cad-pipeline-v0.1 (extractor_version hết trỏ hư không); (2) G3 hết phụ thuộc người nhớ chạy; (3) danh mục vật liệu hết trùng 3 nơi; (4) "pipeline từ chối seed không validated" từ quy ước thành mã máy | pending |
| 2026-07-13 | U1-a: pipeline MỘT LỆNH | scripts/extract/run_pipeline.py | ~180 | field-test Tong lap 2 chiều: mặc định DỪNG đúng Gate G1 (seed BOM chứa Generic) kèm hướng dẫn sửa; --force đi hết chuỗi BOM→STEP→G1→S2(bắt lệch Thân phao 25↔42,5kg)→merge→seed cuối verdict FAIL trung thực | Thay 5–7 lệnh tay + env vars bằng 1 lệnh; người vận hành hết là mắt xích lỗi; tóm tắt cuối in verdict từng seed + lệnh kế tiếp | pending |
| 2026-07-13 | U1-b: chiều dài hàn v0.2 (lỗ hổng dữ liệu định mức) | scripts/extract/weld_length.py | ~200 | selftest giải tích chữ T: 420,0/420,0 mm ĐẠT tuyệt đối; field Tong lap 279 solid: v0 ra 1989m phi lý → v0.2 phân loại bằng thể tích giao: **weld 1614 cặp = 577m** (cận trên, chưa qua G3) + **interference 1274 cặp (= D2-04 đo bằng FreeCAD)** + **209 cặp TRÙNG HÌNH HỌC** (Bich dau dam↔002 chồng khít — nghi đếm trùng khối lượng trong 408kg, giao bên thiết kế rà); quirk freecadcmd nuốt stdout khi có args → mọi tham số qua env | Giờ công hàn = m hàn × định mức: lần đầu có nguồn máy cho biến số giá trị nhất của xưởng; kèm miễn phí detector interference + trùng lặp | pending |
| 2026-07-13 | U1-c: harness tầng 3 — truy vết số đầu ra AI | scripts/extract/trace_numbers.py + nối skills/guard/aigate | ~190 | test cài 3 số bịa giữa 9 số thật (seed Tong lap): v1 lọt "43,9 kg" vì (1) vũ trụ ×10 mù quáng rồi (2) kg khớp nhầm qty=44 → vá 2 tầng: quy đổi THEO ĐƠN VỊ trong doc + khớp ĐÚNG THỨ NGUYÊN (kg→mass, mm→_mm, cái→qty, %→hằng số) → **bắt 3/3, truy được 9/9 số thật**; hiểu số kiểu VN (1.020.575,22 / nhập nhằng chấm) | Rule "mọi số có đơn vị phải truy về seed" chạy bằng máy — đóng cửa số-bịa-trông-giống-thật trước khi người duyệt đọc; exit 2 = chặn phát hành; /aigate check TRACEABILITY giờ có bước máy | pending |
| 2026-07-13 | U1-d: sensor bản vẽ cần Inventor sống | scripts/inventor/Drawing_Check_Live.iLogic.vb | ~170 | CHƯA chạy thật (cần Inventor mở); viết theo bài học 2 lần dính: không Imports System.IO, không Sub lồng trong Main, System.IO.* tường minh, [VER] đánh dấu điểm lệch version | D2-04 AnalyzeInterference + D3-03 dimension dangling + D1-11 BOM view — 3 rule Apprentice không với tới; xuất drawing_live_report.json cùng dạng F01 | pending |
| 2026-07-13 | U2-a: vòng phản hồi thực địa (giai đoạn 4) | schemas/qtcn-actuals.schema.json + scripts/extract/record_actuals.py | ~200 | verified số kiểm tay: hệ số giờ công (15,5+9)/(12+8)=1,225 ✓; suất hàn 62,4/24,5=2,55 m/h ✓; hao hụt 45/38,6=1,166 ✓; NDT PT 2/30=0,067 ✓; record thiếu trường bị schema chặn exit 2 (dữ liệu bẩn không được vào — nó phá hệ số về sau) | Lần đầu có CHỖ CHỨA cho tài sản khó sao chép nhất: actuals.jsonl append-only + calibration.json làm đầu vào lần lập định mức kế; nhóm theo nguyên công+bậc thợ+tư thế hàn khớp giáo trình Bậc 0–5 | pending |
| 2026-07-13 | U2-b: sensor Rhino HU-rules | scripts/extract/rhino_check.py + golden/rhino/ (fixture-good/bad.3dm + generator) | ~180 | fixture cài lỗi: good PASS exit 0; bad bắt 4 FAIL + 3 WARNING đúng chỗ cài (đơn vị Mét, tol 0,5, extrusion hở, thiếu UserText material, layer Default); vào battery → 8/8 | Giới hạn thành thật: rhino3dm không tính mass properties → script lo QUY ƯỚC (HU-01…04) + xuất material-map.json gắn vật liệu vào seed STEP; hình học đi đường STEP AP214 như WX-QT-CAD-IO-01 | pending |
| 2026-07-13 | U2-c: dashboard 1 trang + iLogic D2-05/D3-07 | scripts/extract/pipeline_dashboard.py + Drawing_Check_Live (mở rộng) | ~180 | dashboard chạy thật trên cây Tong lap: 1 dòng trạng thái (bản vẽ ❌ trích xuất ❌ chéo ❌, hàn 577m, INTF 1274, trùng 209, 49 part giao việc) + 6 blocker liệt kê đúng; iLogic D2-05 (occurrence trôi = không ground + không constraint) + D3-07 (đếm weld symbol, [VER] 2 tên collection) CHƯA chạy thật | CEO nhìn 15 giây biết cái gì đang chặn ở đâu; mọi report của 3 tầng harness quy về 1 bảng | pending |
| 2026-07-01 | qtcn / product-dossier (Inventor BOM seed) | scripts/extract/bom_xlsx_to_seed.py | ~230 | verified end-to-end (BOM_demo.xlsx mô phỏng Inventor export: dòng rác trước header + 8 cột Item/Part Number/Description/QTY/Material/Stock Number/Mass(kg)/BOM Structure → auto-nhận header row, map cột chịu tên Anh/Việt bỏ dấu, **bỏ cụm lắp** Frame0001 (BOM Structure=Assembly), 6 VT parts, **est_mass_kg THẬT** từ cột Mass (Σ=28,83 kg), section L40×40×4 + tấm5/8mm rút từ Material+Stock, mass_unit auto kg/g, cp1252-safe reconfigure; flags mass/material/drawing_no đều complete=true). Xuất **cùng schema qtcn-seed/v1** ⇒ drop-in cho merge_qtcn_seeds.py | Inventor BOM (.xlsx/.csv) → qtcn-seed.json: **nguồn vàng** đóng 3 gap của seed suy diễn — khối lượng THẬT (không đoán ρ theo tên như STEP), QTY lắp THẬT (không lệch 1-instance như STEP), drawing_no=Part Number (hết null); biến đổi tất định trong code, không nhập tay BOM/VT/khối lượng mỗi lần | pending |

