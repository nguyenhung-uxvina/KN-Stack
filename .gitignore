# Runtime artifacts
create-junctions.ps1
*.bak
*.tmp

# OS
Thumbs.db
.DS_Store
desktop.ini

# Session/runtime state (written by hooks — not source of truth)
/_meta/
/progress.md

# Vault/editor artifacts that don't belong in the skills repo
.obsidian/

# Book-authoring pipeline output at repo root (generated by codebase-to-book) — NOT skills/book/
/book/

# Binary office artifacts
*.xlsx
*.docx

# Generated eval fixtures (deterministic — regenerated by accuracy-eval.py from the committed generator)
/evals/fixtures/*.dxf
