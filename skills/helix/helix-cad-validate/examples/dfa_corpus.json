{
  "meta": {
    "tool": "helix-cad-validate",
    "note": "Hermetic DFA-only corpus for dfa_part_count. Kept separate from corpus.json so the DFA wire-in proves green independent of the pre-existing EX-*/IE-* fixture gaps (missing pass_case/fail_case/design_rules.example.json). Merge into corpus.json once those base fixtures are restored.",
    "validator": "../validate.py"
  },
  "cases": [
    { "id": "DFA-PASS-clean", "tier": "regression", "extract": "dfa_pass.cad_extract.json",
      "rules": "dfa_rules_gating.json", "expect": "PASS", "origin": "DFA wire-in 2026-07-17",
      "note": "7 parts <=40; fastener 2/7=29%<=30%; DFA index 5/7=0.71>=0.60 → tất cả PASS → gate PASS." },
    { "id": "DFA-FAIL-gating", "tier": "regression", "extract": "dfa_fail.cad_extract.json",
      "rules": "dfa_rules_gating.json", "expect": "FAIL", "origin": "DFA wire-in 2026-07-17",
      "note": "44>40 parts; fastener 36/44=82%; DFA index 1/44=0.02. advisory:false → 3 FAIL → gate FAIL." },
    { "id": "DFA-WARN-advisory", "tier": "regression", "extract": "dfa_fail.cad_extract.json",
      "rules": "dfa_rules_advisory.json", "expect": "PASS", "origin": "DFA wire-in 2026-07-17",
      "note": "Cùng vi phạm nhưng advisory:true → WARN (không gating) → verdict PASS. Chứng minh tầng advisory." },
    { "id": "DFA-FAILSAFE-gating", "tier": "regression", "extract": "dfa_failsafe.cad_extract.json",
      "rules": "dfa_rules_gating.json", "expect": "FAIL", "origin": "DFA wire-in 2026-07-17",
      "note": "min_dfa_index đặt nhưng rows thiếu dfa_essential + require_complete → fail-safe FAIL (gating)." },
    { "id": "DFA-FAILSAFE-advisory", "tier": "regression", "extract": "dfa_failsafe.cad_extract.json",
      "rules": "dfa_rules_advisory.json", "expect": "PASS", "origin": "DFA wire-in 2026-07-17",
      "note": "Cùng thiếu cờ nhưng advisory:true → WARN → verdict PASS (fail-safe chỉ cảnh báo khi advisory)." }
  ]
}
